FROM node:20-alpine AS build
WORKDIR /app

# PRIMI ARG I PRETVORI U ENV ZA BUILD
ARG NEXT_PUBLIC_API_BASE_URL
ENV NEXT_PUBLIC_API_BASE_URL=$NEXT_PUBLIC_API_BASE_URL

COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build
